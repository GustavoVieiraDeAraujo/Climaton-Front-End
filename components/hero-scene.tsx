// Animação real escolhida pelo usuário (LottieFiles "Save Environment", exportada como SVG com
// animação SMIL embutida, árvore crescendo). Hospedada localmente em public/save-environment.svg
// pra não depender de rede externa durante um pitch ao vivo.
export function HeroScene() {
  // eslint-disable-next-line @next/next/no-img-element
  return <img src="/save-environment.svg" alt="" className="hero-scene" />
}
